package kr.or.ddit.notification.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;

import jakarta.servlet.http.HttpSession;
import kr.or.ddit.notification.service.INotificationService;
import lombok.extern.slf4j.Slf4j;




/**
 * <pre>
 * PROJ : Tudio
 * Name : NotificationController
 * DESC : 실시간 알림 컨트롤러 클래스
 * </pre>
 * 
 * @author [대덕인재개발원] team1 KMS
 * @version 1.0, 2025.01.09
 * 
 */
@Slf4j
@Controller
@RequestMapping("/tudio/noti")
public class NotificationController {
	
	
	@Autowired
	private INotificationService service;
	
	
	/**
	 * 알림 목록 조회(드롭다운) => 목록 조회는 최신순으로 기본 정렬
	 * @param model
	 * @param session
	 * @return
	 */
	@GetMapping("/form")
	public String notificationForm(Model model, HttpSession session) {
		log.info("알림창 notificationForm ");
		
		return "";
	}
	
	
	// 안읽음 카운트 조회(배지 숫자)
	
	//알림 읽음 처리(클릭했을때) => 알림을 읽음으로 바꾸고 배지 감소
	
	//알림 삭제(X버튼) => 논리삭제
	
	// 여기부턴 선택사항.
	// 전체 읽음 처리 (UI에 모두 읽음 버튼 만든다면)
	// 전체 삭제 (UI에 모두 삭제 버튼 만든다면)
	
	
	// 30일 자동 삭제는 '컨트롤러'가 아니라 별도 스케쥴러 @scheduled로 하루 한번 돌리거나 DB에서 create_at<sysdate-30조건으로..
	
	// 중요.. 컨트롤러 메서드들은 공통으로 현재 로그인 memberNo를 알아야함. 우린 세션 쓰니까 session에서 memberNo를 꺼내자
	
	
}
