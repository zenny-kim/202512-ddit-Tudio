@charset "UTF-8";


/* =========================================================
   [1] 디자인 토큰 & 초기 설정 (Design Tokens)
   - 사이트 전체에서 공통으로 사용할 색상, 너비, 높이 변수 정의
   - --main-blue: 포인트 컬러 / --border-blue: 연한 테두리
   ========================================================= */
   
/* =========================================================
   [2] 레이아웃 및 전체 구조 (Layout)
   - .main-content-wrap: 사이드바를 제외한 실제 본문 영역
   - .tab-content-card: 탭 아래에 붙는 메인 흰색 판 (둥근 모서리)
   ========================================================= */
   
/* =========================================================
   [3] 탭 메뉴 스타일 (Navigation Tabs)
   - .custom-index-tabs: 대메뉴 탭 (업무, 게시판, 캘린더 등)
   - .board-tabs: 게시판 내부 소분류 (공지, 자유, 회의록 등 둥근 버튼)
   ========================================================= */
   
/* =========================================================
   [4] 카드형 데이터 요약 (Summary Cards)
   - .summary-card: 대시보드나 상단에 수치를 보여줄 때 사용 (진행률 등)
   - .summary-progress-bar: 진행률을 나타내는 막대 그래프 스타일
   ========================================================= */

/* =========================================================
   [5] 공통 버튼 시스템 (Tudio Buttons)
   - .tudio-btn: 모든 버튼의 기본 틀 (여백, 둥글기, 폰트)
   - .tudio-btn-primary: [중요] 등록, 저장, 완료 등 강조할 때 사용
   - .tudio-btn-outline: [일반] 목록, 수정, 취소 등 일반적인 동작
   - .tudio-btn-outline-danger: [주의] 삭제, 거절 등 위험한 동작
   - .tudio-icon-btn: 아이콘만 들어가는 작은 정사각형 버튼
   ========================================================= */

/* =========================================================
   [6] 섹션 및 필터 바 (Section Components)
   - .tudio-section: 게시판 전체를 감싸는 테두리 박스
   - .tudio-section-header: 섹션의 제목과 상단 버튼이 들어가는 영역
   - .tudio-section-filter: 검색창이나 필터가 들어가는 영역
   - .tudio-search: 입력창과 돋보기 아이콘을 하나로 묶은 스타일
   ========================================================= */

/* =========================================================
   [7] 카드형 테이블 & 리스트 (Data Display)
   - .tudio-table-card: 행(Row) 사이 간격이 있고 둥글게 처리된 목록
   - .tudio-item: 자료실 파일이나 팀원 목록 같은 개별 아이템 카드
   - .tudio-badge: 상태값 강조 (공지, 자유, 완료, 대기 등)
   - .tudio-empty: "조회된 데이터가 없습니다" 표시용 점선 박스
   ========================================================= */

/* =========================================================
   [8] 페이지네이션 (Pagination)
   - .tudio-pager: 숫자 버튼들의 묶음 영역
   - .tudio-pg.is-active: 현재 내가 머물고 있는 페이지 번호 강조
   ========================================================= */
  

/* =========================================================
   ✅ Common Design Tokens (공통)
   ========================================================= */
:root {
	--main-blue: #3B82F6;
	--dark-navy: #00407F;
	--header-blue: #D9E9FF;
	/* 프로젝트 공통 테두리 색을 project_common 톤(#BFDBFE)과 통일 */
	--border-blue: #BFDBFE;
	--bg-body: #F8FAFC;
	--text-main: #1E293B;
	--text-muted: #64748B;
	--sidebar-width: 90px; 
  	--header-height: 70px;
}

body {
	background-color: var(--bg-body);
	color: var(--text-main);
}

/* 자주 쓰는 유틸 */
.text-primary-dark {
	color: var(--dark-navy) !important;
}

.bg-light-blue {
	background-color: var(--header-blue) !important;
}

/* 1. 레이아웃 및 공통 설정 */
/* 사이드바 사용 */
.main-content-wrap {
	margin-left: var(--sidebar-width);
	width: calc(100% - var(--sidebar-width));
	min-width: 0;
	transition: margin-left 0.3s ease;
	background-color: #f8fafc;
	min-height: calc(100vh - var(--header-height));
}
/* 탭 내부 레이아웃 사용 */
.main-content-wrap-tap {
	transition: margin-left 0.3s ease;
	background-color: #f8fafc;
}

/* 2. 상단 인덱스 탭 (Project Tabs) */
.custom-index-tabs {
	gap: 2px;
	padding-left: 0; /* 왼쪽 끝에 붙이기 */
}

.custom-index-tabs .nav-link:hover {
	background-color: #e9ecef;
	color: #212529;
}

/* 활성화된 탭 (Active) - 하얀색 배경 고정 */
.custom-index-tabs .nav-link.active {
	background-color: #ffffff !important;
	color: #3B82F6 !important;
	font-weight: 700 !important;
	border-top: 1px solid #BFDBFE !important;
	border-left: 1px solid #BFDBFE !important;
	border-right: 1px solid #BFDBFE !important;
	border-bottom: 1px solid #ffffff !important;
	position: relative;
	z-index: 5;
	margin-bottom: -1px;
}

/* 탭 아래 본문 카드 영역 */
.tab-content-card {
	background: #fff;
	border: 1px solid #BFDBFE;
	border-radius: 0 8px 8px 8px;
	margin-top: -1px;
	padding: 24px;
}

/* 3. 게시판 내부 카테고리 탭 (둥근 버튼 스타일) */
.board-tabs {
	display: inline-flex;
	flex-direction: row;
	align-items: center;
	justify-content: center;
	gap: 8px;
	flex-wrap: wrap;
}

/* ✅ 탭 버튼을 가로 정렬 + 세로 가운데 정렬 */
.custom-index-tabs .nav-link {
	display: inline-flex; /* 또는 flex 도 OK */
	align-items: center;
	gap: 6px; /* 아이콘-글씨 간격 */
	flex-direction: row;
	background-color: #f1f3f5;
	border: 1px solid #BFDBFE !important;
	border-bottom: none !important;
	border-top-left-radius: 10px;
	border-top-right-radius: 10px;
	color: #64748b;
	padding: 12px 18px;
	margin-right: 4px;
	
}

/* ✅ 아이콘은 inline으로 */
.custom-index-tabs .nav-link i {
	font-size: 0.85rem;
	display: inline-flex; /* 기존 display:flex 제거 */
	align-items: center;
	line-height: 1;
	margin-right: 0; /* gap 쓰니까 margin 제거 */
	margin-bottom: 0;
	margin-top: 2px;
	min-width: 24px; /* ✅ 탭 가로 길이(원하는 값으로 조절) */
	justify-content: center; /* ✅ 아이콘+글씨를 가운데 정렬 */
	white-space: nowrap; /* ✅ 글씨 줄바꿈 방지 */
}

.board-tabs .tab {
	border: 1px solid #e6edf6;
	background: #fff;
	padding: 7px 16px;
	border-radius: 999px;
	font-size: 13px;
	color: #495057;
	font-weight: 500;
	transition: .15s ease;
}

.board-tabs .tab:hover {
	background-color: #f8f9fa;
	transform: translateY(-1px);
}

.board-tabs .tab.is-active {
	border-color: #0d6efd;
	background: rgba(13, 110, 253, .08);
	color: #0d6efd;
	font-weight: 700;
}

/* 4. 검색 필터 및 버튼 스타일 */
.board-filter {
	background-color: #f8fafc;
	border: 1px solid #eef2f7;
}

.btn-primary.rounded-pill {
	font-weight: 600;
	padding: 8px 20px;
	box-shadow: 0 4px 12px rgba(13, 110, 253, 0.15);
}

/* 5. 게시판 테이블 (목록) 스타일 */
.table {
	font-size: 0.95rem;
	border-collapse: separate;
}

/* 테이블 헤더 */
.table thead th {
	font-weight: 700;
	color: #495057;
	padding: 15px 10px;
	background-color: #f8f9fa !important;
	border-bottom: 2px solid #eef2f7;
}

/* 테이블 본문 행(Row) 애니메이션 */
.table tbody tr {
	transition: all 0.2s ease;
}

.table tbody tr:hover {
	background-color: #fcfdfe !important;
	transform: scale(1.002);
}

.table tbody td {
	font-weight: 500;
	color: #495057;
	padding: 16px 10px;
	border-bottom: 1px solid #f1f5f9;
}

/* 제목 링크 스타일 */
.table tbody td a {
	color: #1e293b;
	text-decoration: none;
	font-weight: 600;
}

.table tbody td a:hover {
	color: #0d6efd;
}

/* 6. 페이지네이션 (Pagination) */
.board-footer {
	display: flex;
	justify-content: center;
	padding: 20px 0;
}

.pager {
	display: flex;
	gap: 5px;
}

.pg {
	border: 1px solid #dee2e6;
	background: #fff;
	color: #495057;
	padding: 8px 14px;
	border-radius: 8px;
	font-size: 14px;
	text-decoration: none;
	transition: .15s ease;
}

.pg:hover {
	background: #f1f5ff;
	color: #0d6efd;
}

.pg.is-active {
	background: #0d6efd;
	color: #fff;
	border-color: #0d6efd;
	font-weight: 700;
}

/* 카드 레이아웃: 달력의 .calendar-wrapper 스타일 계승 */
.summary-container {
	display: flex;
	gap: 16px;
	margin-bottom: 25px;
}

.summary-card {
	flex: 1;
	background: #ffffff;
	border: 1px solid #BFDBFE; /* 달력의 --border-blue 활용 */
	border-radius: 16px; /* 게시판보다 조금 더 부드러운 라운딩 */
	padding: 20px;
	box-shadow: 0 4px 12px rgba(44, 62, 80, 0.05);
	transition: all 0.2s ease;
}

.summary-card:hover {
	transform: translateY(-3px);
	box-shadow: 0 8px 20px rgba(59, 130, 246, 0.1);
}

/* 카드 내부 라벨 스타일 */
.summary-label {
	display: block;
	font-size: 0.8rem;
	font-weight: 700;
	color: #64748b; /* 게시판 탭 컬러와 통일 */
	margin-bottom: 8px;
}

/* 카드 내부 수치 스타일 */
.summary-value {
	font-size: 1.35rem;
	font-weight: 800;
	color: #002B55; /* 달력 타이틀 컬러와 통일 */
	margin-bottom: 5px;
}

/* 완료율 프로그레스 바: 달력의 --main-blue 활용 */
.summary-progress-wrapper {
	height: 6px;
	background-color: #E2E8F0;
	border-radius: 10px;
	overflow: hidden;
	margin-top: 12px;
}

.summary-progress-bar {
	height: 100%;
	background-color: #3B82F6; /* 달력의 --main-blue */
	border-radius: 10px;
}

/* 배지 및 기타 텍스트 */
.summary-subtext {
	font-size: 0.75rem;
	color: #94a3b8;
}

.summary-badge {
	background-color: #D9E9FF; /* 달력의 --header-blue */
	color: #1E40AF;
	padding: 2px 8px;
	border-radius: 6px;
	font-size: 0.7rem;
	font-weight: 700;
}

.text-danger-custom {
	color: #ef4444 !important;
}

/* =========================================================
   ✅ 아래부터: "탭들 전체 통일"을 위한 공통 컴포넌트(추가분)
   - 기존 CSS 안 건드리고, 필요한 클래스만 추가
   ========================================================= */

/* A) 탭 내부 검색/툴바: 어떤 탭이든 같은 구조로 쓰기 */
.tudio-toolbar {
	display: flex;
	align-items: center;
	justify-content: space-between;
	gap: 12px;
	flex-wrap: wrap;
	margin-bottom: 14px;
}

/* board-filter 스타일을 다른 탭에서도 쓰기 쉬운 별칭 */
.tudio-filter {
	background-color: #f8fafc;
	border: 1px solid #eef2f7;
}

/* 탭 컨텐츠 안의 인풋/셀렉트 톤 통일(범위 제한: tab-content-card 안에서만) */
.tab-content-card .form-control, .tab-content-card .form-select {
	border-radius: 10px;
	border: 1px solid #e6edf6;
	font-weight: 500;
}

.tab-content-card .form-control:focus, .tab-content-card .form-select:focus
	{
	border-color: #BFDBFE;
	box-shadow: 0 0 0 4px rgba(59, 130, 246, .10);
}

/* B) 리스트형 UI(업무/자료실/구성원 등) 공통 */
.tudio-list {
	display: flex;
	flex-direction: column;
	gap: 10px;
}

.tudio-item {
	background: #fff;
	border: 1px solid #e6edf6;
	border-radius: 12px;
	padding: 14px 14px;
	transition: .15s ease;
}

.tudio-item:hover {
	transform: translateY(-1px);
	box-shadow: 0 10px 24px rgba(15, 23, 42, .06);
	border-color: #BFDBFE;
}

.tudio-item-title {
	font-weight: 700;
	color: #1e293b;
}

.tudio-item-sub {
	font-size: .85rem;
	color: #64748b;
}

/* C) 빈 상태(데이터 없을 때) 공통 */
.tudio-empty {
	border: 1px dashed #cbd5e1;
	background: #ffffff;
	border-radius: 16px;
	padding: 26px;
	text-align: center;
	color: #64748b;
}

.tudio-empty i {
	font-size: 28px;
	display: block;
	margin-bottom: 10px;
	color: #94a3b8;
}

.tudio-empty .title {
	font-weight: 700;
	color: #1e293b;
	margin-bottom: 6px;
}

/* =========================================================
   ✅ Common Card / Wrapper (모든 페이지 통일)
   ========================================================= */
.tudio-card, .calendar-wrapper, .filter-wrapper, .post-card, .main-card,
	.project-card {
	background: #ffffff;
	border: 1px solid var(--border-blue) !important;
	border-radius: 12px !important;
	box-shadow: 0 4px 12px rgba(0, 64, 127, 0.05) !important;
}

/* 프로젝트 페이지 main 공통(기존 inline style 제거용) */
.project-main {
	padding-top: 20px;
	min-width: 0;
}

/* 버튼(일정페이지 ‘새 일정 생성’ 같은 버튼 통일) */
.tudio-btn-block {
	padding: 12px 0;
	border-radius: 12px;
	font-size: 0.95rem;
}

/* =========================================================
   ✅ FullCalendar Common
   ========================================================= */
.fc .fc-toolbar-title {
	color: var(--dark-navy);
	font-weight: 700;
}

.fc .fc-col-header-cell {
	background-color: var(--header-blue) !important;
	padding: 10px 0 !important;
}

/* =========================================================
   ✅ Common Modal Skin (모든 모달 톤 통일)
   ========================================================= */
.tudio-modal {
	border-radius: 15px;
	border: none;
	overflow: hidden;
}

.tudio-modal .nav-tabs {
	display: flex;
	width: 100%;
}

.tudio-modal-header {
	background-color: #f8fbff;
	border-bottom: 1px solid var(--border-blue);
}

.tudio-modal-footer {
	background-color: #f8fbff;
	border-top: 1px solid var(--border-blue);
}

.tudio-modal-title {
	color: var(--dark-navy);
}

/* 상세 모달 제목 링크 */
.tudio-detail-title-link {
	color: var(--dark-navy);
	transition: 0.2s;
}

/* =========================================================
   ✅ [ADD] Tab 내부 공통 컴포넌트 (기존 코드 수정 없이 추가)
   ========================================================= */
:root{
  --tudio-primary-strong: #2f6bff; /* btn-write 계열 */
  --tudio-surface: #ffffff;
  --tudio-surface-soft: #fbfcff;

  --tudio-border-soft: #e6edf6;
  --tudio-border-subtle: #eef2f7;

  --tudio-shadow-md: 0 6px 22px rgba(18, 38, 63, 0.06);
  --tudio-shadow-hover: 0 10px 20px rgba(18, 38, 63, 0.08);

  --tudio-radius-lg: 16px;
  --tudio-radius-md: 12px;
  --tudio-ease: .15s ease;
}

/* 탭 컨텐츠 최상단에 class="tudio-scope" 붙이면, 공통 스킨을 그 영역에서만 적용 가능 */
.tudio-scope{ color: var(--text-main); }

/* 섹션 카드(=board-section 대체) */
.tudio-section{
  background: var(--tudio-surface);
  border: 1px solid var(--tudio-border-soft);
  border-radius: var(--tudio-radius-lg);
  box-shadow: var(--tudio-shadow-md);
  overflow: hidden;
  width: 100%;
}

/* 섹션 헤더(=board-header 대체) */
.tudio-section-header{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap: 12px;
  padding: 14px 16px;
  border-bottom: 1px solid var(--tudio-border-subtle);
  background: linear-gradient(180deg, var(--tudio-surface) 0%, var(--tudio-surface-soft) 100%);
}

/* 섹션 필터바(=board-filter 대체) */
.tudio-section-filter{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap: 12px;
  padding: 12px 16px;
  border-bottom: 1px solid var(--tudio-border-subtle);
  background: var(--tudio-surface);
}

/* 공통 버튼 */
.tudio-btn{
  border: 1px solid transparent;
  background: transparent;
  border-radius: var(--tudio-radius-md);
  padding: 10px 14px;
  font-size: 13px;
  font-weight: 600;
  display: inline-flex;
  align-items: center;
  gap: 8px;
  transition: var(--tudio-ease);
  white-space: nowrap;
  cursor: pointer;
}
.tudio-btn-primary{
  background: var(--tudio-primary-strong);
  color: #fff;
  box-shadow: 0 10px 18px rgba(47, 107, 255, .18);
}
.tudio-btn-primary:hover{ transform: translateY(-1px); }

/* ✅ 테두리가 있는 버튼 스타일 추가 */
.tudio-btn-outline {
    border: 1px solid var(--border-blue) !important; /* 프로젝트 공통 테두리 색상 */
    background-color: #ffffff !important;
    color: var(--text-main) !important;
}

.tudio-btn-outline:hover {
    background-color: #f8fafc !important;
    border-color: var(--main-blue) !important;
    color: var(--main-blue) !important;
}

/* 삭제 버튼용 빨간 테두리 */
.tudio-btn-outline-danger {
    border: 1px solid #ef4444 !important;
    background-color: #ffffff !important;
    color: #ef4444 !important;
}

.tudio-btn-outline-danger:hover {
    background-color: rgba(239, 68, 68, 0.05) !important;
    color: #ef4444 !important;
}


/* 검색 아이콘 버튼 */
.tudio-icon-btn{
  border: none;
  background: #f1f5ff;
  color: #1d4ed8;
  width: 34px;
  height: 34px;
  border-radius: 10px;
  display: grid;
  place-items: center;
  transition: var(--tudio-ease);
  cursor: pointer;
}
.tudio-icon-btn:hover{ background: #e7efff; }

/* pills(=board-tabs/tab 대체) */
.tudio-pills{ display:flex; gap: 8px; flex-wrap: wrap; }
.tudio-pill{
  border: 1px solid var(--tudio-border-soft);
  background: var(--tudio-surface);
  padding: 7px 12px;
  border-radius: 999px;
  font-size: 13px;
  color: #334155;
  transition: var(--tudio-ease);
}
.tudio-pill:hover{ transform: translateY(-1px); }
.tudio-pill.is-active{
  border-color: var(--tudio-primary-strong);
  background: rgba(47, 107, 255, .08);
  color: #1d4ed8;
  font-weight: 600;
}

/* 검색 래퍼 */
.tudio-search{
  display:flex;
  align-items:center;
  gap: 8px;
  padding: 0 10px;
  border: 1px solid var(--tudio-border-soft);
  border-radius: var(--tudio-radius-md);
  background: var(--tudio-surface);
  border-radius: 6px;
  height: 31px;	/* 셀렉트 박스 높이와 일치 */
  border: 1px solid #dee2e6;
  border-radius: 0.25rem;
}

.tudio-search input{
  border: none !important;
  box-shadow: none !important;
  outline: none !important;
  background: transparent;
  width: 100%;
  height: 100%;
  font-size: 13px;
}

/* 카드형 테이블 모드(=board-table 대체) */
.tudio-table-card{
  width: 100%;
  border-collapse: separate;
  border-spacing: 0 10px;
}
.tudio-table-card thead th{
  font-size: 12px;
  color: var(--text-muted);
  font-weight: 700;
  text-align: left;
  padding: 10px 12px;
}
.tudio-table-card tbody tr{
  background: var(--tudio-surface);
  border: 1px solid #e9eef5;
  box-shadow: 0 6px 14px rgba(18, 38, 63, 0.04);
  transition: var(--tudio-ease);
}
.tudio-table-card tbody tr:hover{
  transform: translateY(-1px);
  box-shadow: var(--tudio-shadow-hover);
}
.tudio-table-card tbody td{
  padding: 14px 12px;
  border-top: 1px solid var(--tudio-border-subtle);
  border-bottom: 1px solid var(--tudio-border-subtle);
}
.tudio-table-card tbody tr td:first-child{
  border-left: 1px solid var(--tudio-border-subtle);
  border-top-left-radius: 14px;
  border-bottom-left-radius: 14px;
}
.tudio-table-card tbody tr td:last-child{
  border-right: 1px solid var(--tudio-border-subtle);
  border-top-right-radius: 14px;
  border-bottom-right-radius: 14px;
}

/* 배지(bootstrap .badge 충돌 피하려고 별도 클래스) */
.tudio-badge{
  display:inline-flex;
  align-items:center;
  justify-content:center;
  padding: 6px 10px;
  border-radius: 999px;
  font-size: 12px;
  font-weight: 700;
}
.tudio-badge.notice{ background: rgba(239, 68, 68, .10); color: #ef4444; }
.tudio-badge.qna{ background: rgba(245, 158, 11, .12); color: #b45309; }
.tudio-badge.free{ background: rgba(34, 197, 94, .12); color: #15803d; }

/* pager 공통 */
.tudio-pager {
    display: flex;
    justify-content: center;
    gap: 6px;
    flex-wrap: wrap;
    padding: 24px 0;      /* 위아래 여백을 24px로 넉넉히 주어 바닥 선과 띄웁니다 */
    background: #fff;     /* 배경색을 흰색으로 고정 */
}

.tudio-pg {
  border: none;              /* 테두리 제거 */
  background: transparent;   /* 기본 배경 투명 */
  color: #64748b;            /* 기본 글자색 (muted 톤) */
  padding: 8px 14px;
  border-radius: 8px;        /* 약간 덜 둥글게 */
  font-size: 14px;
  transition: all 0.2s ease;
  text-decoration: none;
}

.tudio-pg:hover {
  background: #f1f5f9;       /* 마우스 올리면 연한 회색 */
  color: var(--tudio-primary-strong);
}

.tudio-pg.is-active {
  background: var(--tudio-primary-strong);
  color: #fff;
  font-weight: 700;
  box-shadow: 0 4px 10px rgba(47, 107, 255, 0.2); /* 강조 효과 */
}


/* responsive */
@media (max-width: 768px){
  .tudio-section-filter{ flex-direction: column; align-items: stretch; }
  .tudio-search input{ min-width: 160px; }
}
